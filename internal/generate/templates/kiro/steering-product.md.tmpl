# Product steering ({{ .StackList }})

What this project is and who it serves. Generated by stackup; expand with
product context, goals, and non-goals.
{{- if .Generic }}

No stacks detected yet; keep this generic until `stackup detect` reports.{{- end }}
