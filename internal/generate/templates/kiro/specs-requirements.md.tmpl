# Requirements scaffold ({{ .StackList }})

Seed for `.kiro/specs/` feature work. Generated by stackup; replace with
real requirements.

## Detected stacks

| Ecosystem | Confidence | Signals | Version | Package manager |
|-----------|------------|---------|---------|-----------------|
{{- range .Stacks }}
| {{ .Ecosystem }} | {{ .Confidence }} | {{ .Signals }} | {{ .VersionHint }} | {{ .PackageManager }} |{{- end }}
{{- if .Generic }}

No supported stacks detected. Re-run `stackup detect` after adding a
manifest.{{- end }}

## Functional

- [ ] Describe what the next feature must do.
- [ ] List acceptance criteria in Given/When/Then form.
